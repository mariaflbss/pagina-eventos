export default function NavBar() {
  return (
    <nav
      style={{
        backgroundColor: "#135b78",
        width: "200px",
        minHeight: "100vh",
        color: "white",
        padding: "20px",
      }}
    >
      <h1>Navbar aparece</h1>
    </nav>
  );
}
